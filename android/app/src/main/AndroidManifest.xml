<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <application
        android:label="InTravel"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!--
          Google Maps SDK for Android API key (used by google_maps_flutter
          throughout the app's map screens). Injected at build time from
          android/local.properties' MAPS_API_KEY entry via the
          manifestPlaceholders wiring in android/app/build.gradle.kts —
          never hardcode a real key here directly. local.properties is
          gitignored, so each teammate sets their own key locally
          (MAPS_API_KEY=your_key_here) without it ever being committed.
          If the key is missing/invalid, the app detects the resulting
          map-load failure and shows a graceful "Map unavailable" fallback
          instead of crashing (see lib/screens/osm_poi_map_screen.dart).
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
